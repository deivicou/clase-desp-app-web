<!--
  Fragmento de /opt/tomcat/conf/server.xml con conectores SSL.
  Solo muestra los <Connector> a modificar, no el fichero completo.
-->

<!-- Conector HTTP — redirige HTTPS al puerto 8443 -->
<Connector port="8080" protocol="HTTP/1.1"
           connectionTimeout="20000"
           redirectPort="8443" />


<!-- OPCIÓN A: SSL con keytool (keystore JKS) -->
<Connector port="8443"
           protocol="org.apache.coyote.http11.Http11NioProtocol"
           maxThreads="150" SSLEnabled="true"
           scheme="https" secure="true"
           clientAuth="false"
           sslProtocol="TLS"
           keystoreFile="/etc/ssl/certs/tomcat.keystore"
           keystorePass="123456" />


<!-- OPCIÓN B: SSL con certificado OpenSSL convertido a PKCS12
<Connector port="8443"
           protocol="org.apache.coyote.http11.Http11NioProtocol"
           maxThreads="150" SSLEnabled="true"
           scheme="https" secure="true"
           clientAuth="false"
           sslProtocol="TLS"
           keystoreFile="/opt/tomcat/conf/tomcat.p12"
           keystoreType="PKCS12"
           keystorePass="123456" />
-->


<!-- Conector AJP para mod_jk (descomentar si se usa mod_jk) -->
<!--
<Connector port="8009" protocol="AJP/1.3"
           redirectPort="8443"
           secretRequired="false"/>
-->
